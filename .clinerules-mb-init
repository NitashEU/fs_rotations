extends_rules: ".clinerules"

file_access_and_permissions:
  write_access:
    - "memory-bank/re/*.md"
  exclude:
    - "memory-bank/**/*.md" # Exclude writing to other memory bank folders
  exclusive_access: true       # Requires exclusive access during initialization

tool_access:
  allowed_tools:
    - "Read Files"
    - "Edit Files (Markdown)"
    - "Browser" # Limited tools - documentation and user interaction

documentation_rules:
  initial_project_context_documentation:
    - "MUST document project overview in 'memory-bank/re/projectScope.md' under 'Project Overview' heading."
    - "MUST document existing documentation summaries in 'memory-bank/re/projectScope.md' under 'Existing Documentation Summary' heading (if applicable)."
    - "MUST document initial codebase overview notes in 'memory-bank/re/constraints.md' under 'Codebase Overview - Initial Notes' heading."
    - "MUST document initial requirements in 'memory-bank/re/requirements.md' under 'Initial Requirements' heading (if applicable)."
    - "MUST document known constraints in 'memory-bank/re/constraints.md' under 'Known Constraints - Initial Notes' heading (if applicable)."
  markdown_formatting:
    - "Use clear headings and bullet points for readability."
    - "Maintain a concise and factual documentation style."

behavior_and_constraints:
  focus_on_initial_setup: "Primary responsibility: guide user and document initial project info"
  user_guidance: "Actively guide user through initialization, request info structured"
  confirmation_required: "Get explicit user confirmation on initial setup completion"
  limited_scope: "Mode ONLY for initial setup. Switch to RE mode after setup with tool 'switch_mode' and slug 're'."